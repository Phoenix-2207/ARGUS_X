# ARGUS-X Backend — Slim Deployment Dependencies
# Excludes heavy ML packages (torch, sentence-transformers) to stay under 4GB
# System runs in RULE-ONLY mode without ML — still fully functional

fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.5.0
python-dotenv>=1.0.0

# Supabase
supabase>=2.3.0
postgrest>=0.13.0

# LLM
litellm>=1.16.0

# Utilities
httpx>=0.25.0
aiofiles>=23.2.0
python-multipart>=0.0.6

# Session persistence
redis>=5.0.0

# Lightweight ML (no torch dependency)
numpy>=1.26.0
scikit-learn>=1.3.0

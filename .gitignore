# ARGUS-X Backend
__pycache__/
*.pyc
*.pyo
.env
models/
*.onnx
.DS_Store
venv/
.venv/
node_modules/

# ── Build System Guard ────────────────────────────────────────────────────────
# This project uses ONLY Dockerfile + root railway.json for deployment.
# Block conflicting build configs from being re-committed.
nixpacks.toml
Procfile
argus/backend/railway.json
argus/backend/Procfile

# ── Duplicate Repo Guard ─────────────────────────────────────────────────────
# ARGUS_X/ was a broken inner copy of the entire repo. Do not re-create.
ARGUS_X/

# ── Frontend Build Artifacts ─────────────────────────────────────────────────
argus/frontend/dist/
